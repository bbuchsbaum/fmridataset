% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmri_group.R
\name{fmri_group}
\alias{fmri_group}
\title{Create an fmri_group (one row per subject)}
\usage{
fmri_group(
  subjects,
  id,
  dataset_col = "dataset",
  space = NULL,
  mask_strategy = c("subject_specific", "intersect", "union")
)
}
\arguments{
\item{subjects}{A \code{data.frame} (or tibble) with one row per subject where one
column contains per-subject \code{fmri_dataset} objects stored as a list column.}

\item{id}{Character scalar giving the name of the subject identifier column.}

\item{dataset_col}{Character scalar naming the list column that stores the
per-subject dataset handles.}

\item{space}{Optional character string describing the nominal common space
for all subjects (e.g., "MNI152NLin2009cAsym").}

\item{mask_strategy}{One of "subject_specific", "intersect", or "union"
describing how masks should be handled when combining subjects. This is a
declarative flag only; no resampling is performed by the constructor.}
}
\value{
An object of class \code{fmri_group} that wraps the input table.
}
\description{
Create an fmri_group (one row per subject)
}
