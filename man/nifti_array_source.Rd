% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nifti-array-source.R
\name{nifti_array_source}
\alias{nifti_array_source}
\title{Construct a pushdown-aware NIfTI array source}
\usage{
nifti_array_source(paths, mask, chunks = NULL)
}
\arguments{
\item{paths}{One or more NIfTI files with a common spatial grid.}

\item{mask}{A NIfTI mask path or a compatible \code{volume_space}.}

\item{chunks}{Optional logical observation-by-feature chunk hint.}
}
\value{
A serializable \code{nifti_array_source}.
}
\description{
The descriptor reads headers and one mask at construction, but no fMRI
volumes. Numerical reads split requested global observations by file, pass
local volume indices into \code{\link[neuroim2:read_vec]{neuroim2::read_vec()}}, and restrict the mask to
requested packed features before materialization. Native reads return
full-volume \code{NeuroVec} objects in requested observation order.
}
\details{
The fingerprint covers the descriptor and the size and modification time of
every file captured at construction, never the voxel values. Every open,
read, and native read re-observes those files first and raises
\code{fmridataset_error_source_stale} (with \code{source}, \code{expected}, \code{actual}, and
\code{changed} fields) if any differ; genuine read failures remain
\code{fmridataset_error_backend_io}. See \code{\link[=content_hash]{content_hash()}} to identify values.
}
\examples{
# A small NIfTI fixture shipped with neuroim2 stands in for real data.
path <- system.file("extdata", "global_mask_v4.nii", package = "neuroim2")
if (nzchar(path)) {
  src <- nifti_array_source(path, path)
  source_shape(src)
  source_dtype(src)
}
}
