% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmri_series.R
\name{fmri_series}
\alias{fmri_series}
\alias{fmri_series.fmri_dataset}
\alias{fmri_series.fmri_study_dataset}
\title{Query fMRI Time Series}
\usage{
fmri_series(dataset, selector = NULL, timepoints = NULL,
            output = c("FmriSeries", "DelayedMatrix"),
            event_window = NULL, ...)
}
\arguments{
\item{dataset}{An \code{fmri_dataset} or \code{fmri_study_dataset}.}
\item{selector}{Spatial selector or \code{NULL} for all voxels.}
\item{timepoints}{Integer or logical vector or \code{NULL} for all.}
\item{output}{Return type.}
\item{event_window}{Reserved for future use.}
\item{...}{Additional backend-specific options.}
}
\value{
Either an \code{FmriSeries} object or a \code{DelayedMatrix}.
}
\description{
Core interface for retrieving voxel time series from fMRI datasets.
}
\examples{
# Create a small example dataset
mat <- matrix(rnorm(40), nrow = 5, ncol = 8)
backend <- matrix_backend(mat, mask = rep(TRUE, 8), spatial_dims = c(2,2,2))
ds <- fmri_dataset(backend, TR = 1, run_length = 5)

# Query two voxels and three timepoints
fs <- fmri_series(ds, selector = 3:4, timepoints = 2:4)
print(fs)
}
