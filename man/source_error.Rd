% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/source-conditions.R
\name{source_error}
\alias{source_error}
\title{Build the structured conditions an array source may signal}
\usage{
source_error(message, type = c("stale", "io", "contract"), ...)
}
\arguments{
\item{message}{One-sentence description of the failure.}

\item{type}{One of \code{"stale"}, \code{"io"}, or \code{"contract"}.}

\item{...}{Named fields stored on the condition, such as \code{source},
\code{expected}, \code{actual}, \code{file}, \code{operation}, or \code{field}.}
}
\value{
A condition object inheriting from the type-specific class,
\code{fmridataset_error}, \code{error}, and \code{condition}.
}
\description{
Storage packages implement the \link[=array-source]{array source} protocol and
must fail the way built-in sources fail, so that callers can handle every
source alike. \code{source_error()} builds the condition object for the three
failure kinds the protocol defines; signal it with \code{stop()}.
}
\details{
\itemize{
\item \code{"stale"} (\code{fmridataset_error_source_stale}): the physical store no longer
matches the descriptor's revision evidence (a file was rewritten, a store
was replaced). Supply \code{source}, \code{expected}, and \code{actual} so the caller can
report what changed.
\item \code{"io"} (\code{fmridataset_error_backend_io}): a genuine read, open, or close
failure. Supply \code{file} and \code{operation} where known.
\item \code{"contract"} (\code{fmridataset_error_source_contract}): the descriptor or a
method violates the protocol. Supply \code{field}.
}

Every condition also carries the \code{fmridataset_error} class, so a single
handler can catch all package errors.
}
\examples{
cond <- source_error(
  "Store was rewritten after the descriptor was built.",
  type = "stale", source = "example.h5",
  expected = "abc", actual = "def"
)
inherits(cond, "fmridataset_error_source_stale")
tryCatch(stop(cond), fmridataset_error_source_stale = function(e) e$actual)
}
