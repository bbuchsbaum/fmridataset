% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal-schema.R
\name{temporal-schema}
\alias{temporal-schema}
\alias{temporal_schema}
\alias{has_temporal_schema}
\alias{as_sampling_frame}
\title{The observation-axis temporal contract}
\usage{
temporal_schema(x, run_col = NULL, tr_col = "TR", censor_col = "censor")

has_temporal_schema(x, ...)

as_sampling_frame(x, ...)
}
\arguments{
\item{x}{An \code{fmri_frame} or \code{fmri_view}.}

\item{run_col, tr_col, censor_col}{Observation metadata columns holding the
run label, repetition time, and censoring indicator.}

\item{...}{Passed to \code{temporal_schema()}.}
}
\value{
\code{temporal_schema()} returns a \code{frame_temporal_schema}.
\code{has_temporal_schema()} returns a scalar logical.
\code{as_sampling_frame()} returns an \code{fmrihrf::sampling_frame}.
}
\description{
The canonical frame model carries no acquisition timing of its own: an
\code{fmri_frame} is observations by features, and when the observations happen to
be volumes acquired in runs, that fact is ordinary observation metadata. The
package already writes it that way -- \code{read_bids_bold()} emits \code{run_id} and
\code{TR} columns -- but nothing validated the convention, so every consumer
reinvented it and none could rely on it.
}
\details{
These functions make it a contract. \code{temporal_schema()} derives a validated
description from the observation metadata; \code{as_sampling_frame()} reconstructs
the \code{fmrihrf::sampling_frame} that the legacy accessors and the design
machinery expect.

The schema is derived, never stored. The columns are the truth, so the schema
cannot go stale, needs no serialization of its own, and follows subsetting,
reordering, and binding for free.
}
\section{Contract}{

\describe{
\item{\code{run_id}}{Required. One value per observation naming the acquisition
run it belongs to. Any type; compared as character. No missing values.}
\item{\code{TR}}{Optional. Repetition time in seconds, positive and finite, and
constant within each run. Runs may differ from one another, matching
\code{fmrihrf::sampling_frame()}.}
\item{\code{censor}}{Optional. Logical, one value per observation, \code{TRUE} where
the observation is to be excluded. No missing values.}
}
}

\section{Order and contiguity}{

Runs are numbered in order of first appearance, not by sorting, so
\code{block_ids} is stable under any operation that preserves observation order.
A frame is \emph{contiguous} when each run occupies one unbroken stretch of
observations. Frames are not required to be contiguous -- \code{filter_obs()} and
ID-based reordering both produce legal interleaved views -- but a
\code{sampling_frame} is a run-length encoding and cannot represent one, so
\code{as_sampling_frame()} refuses a non-contiguous frame rather than silently
reordering it.
}

\examples{
frame <- fmri_frame(
  assays = list(bold = matrix(rnorm(12), 6, 2)),
  observations = data.frame(
    .obs_id = sprintf("t\%02d", 1:6),
    run_id = rep(c("run-1", "run-2"), each = 3),
    TR = 2
  )
)
schema <- temporal_schema(frame)
schema$run_lengths
as_sampling_frame(frame)
}
