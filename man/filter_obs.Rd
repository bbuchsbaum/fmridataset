% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frame-view.R
\name{filter_obs}
\alias{filter_obs}
\title{Filter frame observations using scalar metadata}
\usage{
filter_obs(x, predicate, resolve = TRUE)
}
\arguments{
\item{x}{An \code{fmri_frame} or view.}

\item{predicate}{A metadata expression returning one logical value per
observation.}

\item{resolve}{Whether the predicate may use namespaced entity metadata.}
}
\value{
An \code{fmri_view}.
}
\description{
Filter frame observations using scalar metadata
}
\examples{
sp <- volume_space(dim = c(2, 2, 2), affine = diag(4))
frame <- fmri_frame(
  assays = list(bold = matrix(rnorm(4 * n_features(sp)), nrow = 4)),
  observations = data.frame(
    .obs_id = sprintf("vol-\%d", 1:4),
    run_id = rep(c("run-1", "run-2"), each = 2)
  ),
  space = sp
)
filter_obs(frame, run_id == "run-1")
}
