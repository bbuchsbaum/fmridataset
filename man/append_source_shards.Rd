% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/array-source.R
\name{append_source_shards}
\alias{append_source_shards}
\title{Append immutable source shards}
\usage{
append_source_shards(x, sources, shard_ids = NULL, shard_data = NULL)
}
\arguments{
\item{x}{An existing \code{row_sharded_source}.}

\item{sources}{New compatible child sources.}

\item{shard_ids}{Stable IDs for the new shards.}

\item{shard_data}{Optional metadata for the new shards. Its columns must
match existing shard metadata.}
}
\value{
A new \code{row_sharded_source}; \code{x} and its child descriptors are not
modified.
}
\description{
Append immutable source shards
}
\examples{
src <- row_sharded_source(list(memory_source(matrix(1:4, nrow = 2))))
grown <- append_source_shards(src, list(memory_source(matrix(5:8, nrow = 2))))
source_shape(grown)
}
