% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/content-hash.R
\name{content_hash}
\alias{content_hash}
\alias{content_hash.default}
\alias{content_hash.array_source}
\alias{content_hash.memory_source}
\alias{content_hash.source_view}
\alias{content_hash.row_bound_source}
\title{Compute a numerical content hash of an array source}
\usage{
content_hash(x, ...)

\method{content_hash}{default}(x, ...)

\method{content_hash}{array_source}(
  x,
  ...,
  block_bytes = getOption("fmridataset.target_block_bytes", 4 * 1024^2)
)

\method{content_hash}{memory_source}(
  x,
  ...,
  block_bytes = getOption("fmridataset.target_block_bytes", 4 * 1024^2)
)

\method{content_hash}{source_view}(
  x,
  ...,
  block_bytes = getOption("fmridataset.target_block_bytes", 4 * 1024^2)
)

\method{content_hash}{row_bound_source}(
  x,
  ...,
  block_bytes = getOption("fmridataset.target_block_bytes", 4 * 1024^2)
)
}
\arguments{
\item{x}{An array source, a handle, or a matrix coercible to a memory
source.}

\item{...}{Passed to methods.}

\item{block_bytes}{Upper bound on the realized bytes of one read. The
default reuses the block-planning option
\code{fmridataset.target_block_bytes}.}
}
\value{
One lowercase SHA-256 hexadecimal string.
}
\description{
\code{content_hash()} is the one operation in the package that identifies an
array by its values. It is the content domain of the identity contract
(see \code{\link[=identity_descriptor]{identity_descriptor()}}) and it is always explicit: nothing in the
package computes it for you. Ordinary construction, inspection,
fingerprinting, planning, and persistence never call it.
}
\details{
The digest is \code{O(n)} in the number of values. The default method streams:
it opens one handle, reads bounded row blocks through the source protocol
aligned to the source chunk grid, and hashes each block into a running
state, so no source is materialized whole. \code{block_bytes} bounds the
realized size of one read and affects only I/O; every block size produces
the same digest. Blocks cover whole rows when a row fits the bound and
consecutive feature ranges of one row otherwise.

Two sources hash equal exactly when they have the same shape, realize to the
same R mode (double, logical, or complex), and hold equal values. Storage
dtype, chunking, wrappers, views, and shard composition do not enter the
digest, so an in-memory copy and a row-bound composition of the same values
agree. \code{NaN} payloads and \code{NA} are normalized to one representation each and
remain distinct; negative zero stays distinct from zero.

The digest describes the values at the moment they were read. It is a
receipt, not a live property: a later in-memory mutation of a memory
source's payload is not detected by \code{\link[=source_fingerprint]{source_fingerprint()}} and shows only
when a fresh \code{content_hash()} is compared with the earlier one.
}
\examples{
a <- memory_source(matrix(1:6, 2, 3))
b <- memory_source(matrix(as.double(1:6), 2, 3), chunks = c(1, 1))
identical(source_fingerprint(a), source_fingerprint(b))
identical(content_hash(a), content_hash(b))
}
\seealso{
\code{\link[=source_fingerprint]{source_fingerprint()}} for the cheap revision identity of a
descriptor, \code{\link[=identity_descriptor]{identity_descriptor()}} for typed identity domains, and
\code{inst/architecture/ADR-009-source-fingerprints-and-content-hashes.md}
for the policy.
}
