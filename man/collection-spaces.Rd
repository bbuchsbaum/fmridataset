% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmri-collection.R
\name{collection-spaces}
\alias{collection-spaces}
\alias{collection_space_data}
\alias{collection_common_space}
\title{Summarize collection feature spaces}
\usage{
collection_space_data(x)

collection_common_space(x)
}
\arguments{
\item{x}{An \code{fmri_collection}.}
}
\value{
\code{collection_space_data()} returns one metadata row per frame;
\code{collection_common_space()} returns whether every feature space is exactly
compatible with the first.
}
\description{
Summarize collection feature spaces
}
\examples{
voxels <- volume_space(dim = c(2, 2, 1), affine = diag(4), template = "toy")
frame <- fmri_frame(
  assays = list(bold = matrix(rnorm(3 * n_features(voxels)), nrow = 3)),
  observations = data.frame(.obs_id = paste0("vol-", 1:3)),
  space = voxels
)
collection <- fmri_collection(list(sub01 = frame, sub02 = frame))
collection_space_data(collection)
collection_common_space(collection)
}
