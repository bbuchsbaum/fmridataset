% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fds-schema.R
\name{frame_from_fds_manifest}
\alias{frame_from_fds_manifest}
\title{Reconstruct a frame from an FDS manifest and physical sources}
\usage{
frame_from_fds_manifest(manifest, bindings)
}
\arguments{
\item{manifest}{A valid FDS v1 frame manifest.}

\item{bindings}{Named physical array payloads or \code{array_source} descriptors,
one per manifest array declaration.}
}
\value{
An \code{fmri_frame} whose semantic state comes from \code{manifest} and whose
lazy arrays come from \code{bindings}.
}
\description{
Reconstruct a frame from an FDS manifest and physical sources
}
\examples{
voxels <- volume_space(dim = c(2, 2, 1), affine = diag(4), template = "toy")
frame <- fmri_frame(
  assays = list(bold = matrix(rnorm(3 * n_features(voxels)), nrow = 3)),
  observations = data.frame(.obs_id = paste0("vol-", 1:3)),
  space = voxels
)
manifest <- fds_frame_manifest(frame)
bindings <- fds_frame_bindings(frame)
rebuilt <- frame_from_fds_manifest(manifest, bindings)
identical(feature_ids(rebuilt), feature_ids(frame))
}
