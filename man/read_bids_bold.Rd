% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-bids-bold.R
\name{read_bids_bold}
\alias{read_bids_bold}
\title{Read one subject's preprocessed BIDS BOLD data as an fmri_frame}
\usage{
read_bids_bold(
  path,
  subject,
  task = NULL,
  session = NULL,
  run = NULL,
  space = NULL,
  derivative = "fmriprep",
  mask = "intersection",
  events = TRUE,
  chunks = NULL
)
}
\arguments{
\item{path}{Path to a BIDS dataset containing fMRIPrep derivatives.}

\item{subject}{One exact subject label, with or without the \verb{sub-} prefix.}

\item{task, session, run, space}{Optional exact BIDS entity selectors.}

\item{derivative}{Currently only \code{"fmriprep"}.}

\item{mask}{\code{"intersection"} (default), an explicit NIfTI mask path, or a
compatible \code{volume_space}.}

\item{events}{Whether to attach matching BIDS events as a keyed event table.}

\item{chunks}{Optional observation-by-feature chunk hint passed to
\code{\link[=nifti_array_source]{nifti_array_source()}}.}
}
\value{
A lazy \code{fmri_frame} with a \code{signal} assay.
}
\description{
\code{read_bids_bold()} is the narrow BIDS on-ramp to the canonical frame API. It
discovers fMRIPrep \code{desc-preproc} BOLD runs, creates deterministic volume
IDs, resolves one common volume space, and returns a lazy NIfTI-backed frame.
BOLD values are not read until an assay or spatial map is explicitly
collected.
}
\examples{
\dontrun{
bold <- read_bids_bold(
  "/data/my-study",
  subject = "01",
  task = "memory",
  space = "MNI152NLin2009cAsym"
)
first_run <- filter_obs(bold, run_id == "run-1")
map <- spatial_map(bold, observation = 1)
}
}
