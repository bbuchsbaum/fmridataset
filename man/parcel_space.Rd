% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature-space.R
\name{parcel_space}
\alias{parcel_space}
\title{Construct a parent-linked parcel feature space}
\usage{
parcel_space(
  parent,
  parcel_ids,
  membership,
  data = NULL,
  atlas,
  aggregation = c("mean", "sum"),
  decoder = NULL,
  metadata = list()
)
}
\arguments{
\item{parent}{Parent \code{feature_space} (usually a volume or surface space).}

\item{parcel_ids}{Stable atlas-native parcel identifiers.}

\item{membership}{Non-negative parent-feature by parcel membership weights.}

\item{data}{One metadata row per parcel. The \code{id}, \code{label}, and \code{hemi}
conventions match \code{neuroatlas::parcel_data} when present.}

\item{atlas}{Atlas identity list containing at least \code{id}, or a scalar ID.}

\item{aggregation}{Either weighted \code{"mean"} or weighted \code{"sum"}.}

\item{decoder}{Optional parent-feature by parcel reconstruction operator.
The default blends overlapping parcel values by row-normalized membership.}

\item{metadata}{Additional serializable metadata.}
}
\value{
A \code{parcel_space}.
}
\description{
A \code{parcel_space} owns only the feature-axis algebra required by
\code{fmridataset}: stable parcel identity, a parent feature space, and explicit
parent-to-parcel operators. Atlas discovery, labels, and provenance remain
owned by \code{neuroatlas} and can be imported with \code{\link[=parcel_space_from_atlas]{parcel_space_from_atlas()}}.
}
