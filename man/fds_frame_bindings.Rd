% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fds-schema.R
\name{fds_frame_bindings}
\alias{fds_frame_bindings}
\title{Extract physical array bindings from a frame}
\usage{
fds_frame_bindings(x)
}
\arguments{
\item{x}{An \code{fmri_frame}.}
}
\value{
A named list of \code{array_source} descriptors keyed exactly like the
manifest \code{arrays} registry.
}
\description{
Storage codecs use this helper to pair each source-free FDS array declaration
with its current physical or in-memory \code{array_source}.
}
\examples{
voxels <- volume_space(dim = c(2, 2, 1), affine = diag(4), template = "toy")
frame <- fmri_frame(
  assays = list(bold = matrix(rnorm(3 * n_features(voxels)), nrow = 3)),
  observations = data.frame(.obs_id = paste0("vol-", 1:3)),
  space = voxels
)
bindings <- fds_frame_bindings(frame)
names(bindings)
}
