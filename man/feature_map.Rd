% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature-map.R
\name{feature_map}
\alias{feature_map}
\title{Describe an explicit transformation between feature spaces}
\usage{
feature_map(
  from,
  to,
  operator,
  map_type = "linear",
  traits = list(linear = TRUE),
  provenance = list(),
  metadata = list()
)
}
\arguments{
\item{from}{Source \code{feature_space}.}

\item{to}{Target \code{feature_space}.}

\item{operator}{Target-by-source matrix, sparse \code{Matrix}, or serializable
two-dimensional \code{array_source}.}

\item{map_type}{Stable map-family label.}

\item{traits}{Named serializable semantic traits.}

\item{provenance}{Serializable derivation metadata for the map itself.}

\item{metadata}{Additional serializable metadata.}
}
\value{
A serializable \code{feature_map} descriptor.
}
\description{
A feature map owns a target-by-source linear operator and the complete
spatial identity of both axes. Equal dimensions are never treated as
evidence of spatial compatibility. Statistical execution plans and
covariance models remain the responsibility of packages such as
\code{fmrigds}.
}
\examples{
src <- index_space(4, ids = paste0("v", 1:4), namespace = "map-source")
tgt <- index_space(2, ids = paste0("p", 1:2), namespace = "map-target")
op <- matrix(c(0.5, 0.5, 0, 0, 0, 0, 0.5, 0.5), nrow = 2, byrow = TRUE)
m <- feature_map(src, tgt, op, map_type = "toy_aggregation")
feature_map_operator(m)
}
