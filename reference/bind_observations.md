# Bind frames along observations

Bind frames along observations

## Usage

``` r
bind_observations(...)
```

## Arguments

- ...:

  Frames with identical feature IDs, spaces, and assay names.

## Value

A lazily row-bound `fmri_frame`.
