# Project data onto a shared latent template and recover any scan offset

Project data onto a shared latent template and recover any scan offset

## Usage

``` r
.project_template_with_offset(template, data, template_loadings)
```
