# Compute a deterministic collection digest

Compute a deterministic collection digest

## Usage

``` r
collection_digest(x)
```

## Arguments

- x:

  An `fmri_collection`.

## Value

A SHA-256 digest computed without reading numerical arrays.
