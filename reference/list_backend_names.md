# List Registered Backend Names

Returns a character vector of all registered backend names.

## Usage

``` r
list_backend_names()
```

## Value

Character vector of backend names

## Examples

``` r
list_backend_names()
#> [1] "h5"     "latent" "matrix" "nifti"  "study"  "zarr"  
```
