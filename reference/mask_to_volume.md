# Convert Logical Vector to Volume

Converts a logical vector mask to a 3D array.

## Usage

``` r
mask_to_volume(mask_vec, dims)
```

## Arguments

- mask_vec:

  A logical vector

- dims:

  Spatial dimensions (length 3)

## Value

A 3D logical array
