# Read shared template loadings

Read shared template loadings

## Usage

``` r
.read_template_loadings(h5_handle)
```

## Arguments

- h5_handle:

  An open
  [`hdf5r::H5File`](http://hhoeflin.github.io/hdf5r/reference/H5File-class.md)
  object.

## Value

A numeric matrix `[V, K]` or `NULL`.
