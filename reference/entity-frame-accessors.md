# Entity-frame accessors

Entity-frame accessors

## Usage

``` r
entity_key(x)

entity_ids(x)

entity_data(x)

entity_blocks(x)
```

## Arguments

- x:

  An `entity_frame`.

## Value

The stable key name, entity IDs, scalar data, or aligned blocks.
