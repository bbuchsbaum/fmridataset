# Compute the deterministic digest of a hierarchy index

Compute the deterministic digest of a hierarchy index

## Usage

``` r
hierarchy_digest(x)
```

## Arguments

- x:

  An `fmri_hierarchy_index`.

## Value

A SHA-256 digest.
