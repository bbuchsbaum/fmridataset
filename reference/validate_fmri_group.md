# Validate an fmri_group object

Validate an fmri_group object

## Usage

``` r
validate_fmri_group(x)
```

## Arguments

- x:

  Object to validate.

## Value

The validated `fmri_group` (invisibly).
