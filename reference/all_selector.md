# All Voxels Series Selector

Select all voxels in the dataset mask.

## Usage

``` r
all_selector()
```

## Value

An object of class `all_selector`

## Examples

``` r
# Select all voxels
sel <- all_selector()
```
