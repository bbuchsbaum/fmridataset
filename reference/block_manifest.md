# Inspect a frame block plan

Inspect a frame block plan

## Usage

``` r
block_manifest(plan)
```

## Arguments

- plan:

  A `frame_block_plan`.

## Value

A data frame containing logical block bounds and byte estimates.
