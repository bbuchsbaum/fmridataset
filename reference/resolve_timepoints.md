# Resolve Timepoint Selection

Resolve Timepoint Selection

## Usage

``` r
resolve_timepoints(dataset, timepoints)
```

## Arguments

- dataset:

  An `fmri_dataset` object.

- timepoints:

  Integer or logical vector of timepoints, or `NULL` for all.

## Value

Integer vector of timepoint indices.
