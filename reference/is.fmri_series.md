# Check if object is an fmri_series

Check if object is an fmri_series

## Usage

``` r
is.fmri_series(x)
```

## Arguments

- x:

  An object to test

## Value

Logical TRUE if x is an fmri_series object
