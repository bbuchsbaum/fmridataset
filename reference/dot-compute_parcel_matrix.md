# Compute parcel-averaged time series matrix

Returns a `[T, K]` numeric matrix by averaging voxel time series within
each parcel.

## Usage

``` r
.compute_parcel_matrix(nvec, clusters, cluster_ids, summary_fun)
```
